# 业务部实习生成长导航智能看板 - 方案说明

## 一、问题诊断

20名校招实习生分布在研发、产品、销售等岗位，业务部面临三大痛点：

1. **导师带教缺乏标准化**：导师凭经验带教，节奏参差不齐，部分实习生被"放养"
2. **实习生成长迷茫**："我到底该学什么"成为高频问题，频繁私聊HR询问进度
3. **HR无法全局掌握适岗情况**：招聘同学追问"这批人最近怎么样"，但信息分散在导师手中

## 二、方案设计

核心思路是**标准化+可视化+智能化**：

- **标准化**：按岗位预设12周带教计划，明确每阶段目标、任务、验收标准
- **可视化**：三角色看板——实习生看成长路线、导师看管理面板、HR看数据大盘
- **智能化**：内置AI助手，根据角色和场景提供个性化回复；实习生写周记后AI自动生成反馈

技术方案选择单页Web应用（HTML/CSS/JS），数据通过localStorage本地持久化，零后端依赖，部署至腾讯云CloudBase即可公网访问。

## 三、AI工具选型理由

本项目深度应用AI能力于三个场景，均选择**规则引擎+模板替换**的模拟方案：

| 场景 | 选型 | 理由 |
|------|------|------|
| AI智能问答 | 关键词匹配+上下文感知 | 无需API密钥即可演示，降低部署门槛，回复内容基于真实业务数据动态生成 |
| 日志智能反馈 | 多维度模板评分 | 实习生提交周记后，AI从"学习态度/技术能力/业务理解/团队协作/主动思考"五维度给出个性化鼓励+改进建议 |
| 数据智能预警 | 多因子风险模型 | 综合评分均值、日志提交延迟、任务完成率、导师评语覆盖率四维度加权计算风险分，自动标红异常人员 |

该方案兼顾演示真实感与可落地性，避免了外部API依赖导致的网络延迟和密钥管理问题。未来可平滑升级至真实大模型API。

## 四、关键配置

- **前端框架**：原生HTML5 + CSS3 + JavaScript（无框架，轻量快速）
- **图表库**：Chart.js 4.4.1（CDN引入，支持雷达图、柱状图、饼图、折线图趋势分析）
- **数据持久化**：localStorage单键存储（`tcamp_intern_dashboard_v1`），含实习生/导师/任务/日志/聊天记录/**评分历史(scoreHistory)**
- **权限控制**：角色密码验证（导师密码`mentor2025`、HR密码`hr2025`），sessionStorage会话级授权，实习生免密直接访问
- **数据管理**：支持JSON全量导出/导入备份，HR端提供批量实习生导入（粘贴JSON数组即可）
- **通知系统**：基于当前时间+数据状态实时计算，Header铃铛红点提示逾期任务、日志拖欠、新评论、风险预警
- **后端架构**：CloudBase Serverless 云函数（Node.js 18.15），统一入口处理 CRUD 操作
- **数据库**：CloudBase 文档型数据库（MongoDB 兼容），集合：`interns`（实习生）、`mentors`（导师）、`app_config`（配置）
- **通信方式**：前端通过 CloudBase JS SDK `callFunction` 调用云函数，无需处理 HTTP 跨域
- **部署配置**：腾讯云CloudBase静态托管（前端）+ 云函数（后端）+ 数据库，同环境 ID 内协同；GitHub源码仓库备份，HTTPS公网域名访问
- **主题系统**：CSS Variables定义腾讯蓝（`#0052D9`）为主色，适配/关注/风险三色状态体系

## 五、迭代记录与效果评估

**迭代记录**：
- V1.0：完成三角色基础看板（路线图、任务卡、统计图表）
- V1.1：新增"成长日志"模块，支持结构化周记（本周总结/遇到的困难/下周计划）
- V1.2：接入AI助手，实现日志智能反馈和三角色问答
- V1.3：优化移动端响应式布局，部署至公网
- **V2.0（交互升级）**：
  - 安全层：角色密码验证（导师/HR）+ sessionStorage会话授权
  - 数据层：JSON导出/导入备份 + 批量实习生JSON导入
  - 交互层：日志评论回复（导师↔实习生双向沟通）+ 评价过程评语
  - 通知层：Header铃铛通知（逾期任务、日志提醒、新评论、风险预警）
  - 分析层：评分历史数据模型 + 趋势折线图 + 多因子风险模型
- **V3.0（架构升级）**：
  - 后端架构：CloudBase Serverless 云函数 + 文档型数据库
  - 数据库层：interns/mentors/app_config 三集合，20名实习生+5名导师数据云端持久化
  - 前端改造：Storage.js 异步化，支持 CloudBase JS SDK 调用 + localStorage 降级兜底
  - 部署架构：静态托管 + 云函数双服务，同环境 ID 内协同运行
- **V3.1（Bug修复与体验优化）**：
  - 日志模块修复：`generateLogs()` 预生成范围从 `currentWeek - 1` 改为 `currentWeek - 2`，最新一周留白由实习生亲手填写，解决"实习生没有写日志入口"的问题
  - 数据迁移：云函数 `initData` 增加 `fixPreGeneratedLogs()` 幂等修复，自动清理已存在数据中的最新周预生成日志
  - 前端加载修复：`loadData()` 改为调用 `Storage._getAllData()` 异步加载，修复本地预览/无网络环境下页面卡在"加载中"的问题
  - 部署工具链：新增 `cloudbaserc.json` 配置文件，支持 `tcb fn deploy` 和 `tcb hosting deploy` 一键部署
- **V3.2（AI反馈与本地存储修复）**：
  - 移除 `loadData()` 中冗余的前端自动修复逻辑，修复提交日志后 AI 反馈被意外清除的问题
  - 修复 `Storage.addLog`、`updateTask`、`addLogComment` 本地模式下 `getIntern` 返回 Promise 未 await 导致的 TypeError
  - 前端增加调试日志输出，便于排查日志提交与 AI 反馈生成链路
  - 版本号提升至 `?v=3.2`，强制刷新 CDN 与浏览器缓存
- **V3.3（AI反馈智能化升级）**：
  - 重写 `generateLogFeedback()` 规则引擎：从纯模板轮询升级为**关键词匹配 + 内容分析 + 岗位定制**
  - 新增6大能力维度关键词识别（技术攻坚/产品思维/业务拓展/沟通协作/主动学习/Owner意识），根据用户输入动态匹配闪光点
  - 新增6类困难场景智能建议（新领域/时间管理/沟通协调/技术难点/心理压力/需求变更），针对性输出 actionable advice
  - 内容充实度评级：根据总结长度自动判断"偏简短/比较清晰/非常充实"并给出改进建议
  - 岗位专属小贴士：研发/产品/销售三角色分别输出差异化成长建议
  - 提交成功增加绿色 Toast 提示，提升操作反馈感
  - 版本号提升至 `?v=3.3`
- **V3.4（提交无响应问题根因修复）**：
  - 为 `submitLog()` 添加完整的 `try-catch` 异常捕获，提交失败时弹窗提示具体错误信息，杜绝"点了没反应"的静默失败
  - 增加9步执行链路调试日志（`[submitLog] Step 1-9`），便于快速定位卡点
  - 增加防御性检查：提交前校验 `intern` 对象是否存在，防止数据未加载时提交报错
  - `showToast` 不存在时降级为 `alert`，兼容缓存旧代码场景
  - 版本号提升至 `?v=3.4`
- **V3.5（本地存储 async/await 链式 Bug 彻底修复）**：
  - 修复 `Storage.getIntern()` 本地模式下调用异步 `this.getInterns()` 但未 await 导致 `TypeError: ...find is not a function` 的致命错误
  - 修复 `Storage.getMentor()` 本地模式下同样的 async 链式调用 bug
  - 修复 `API.ensureAuth()` 中 `auth.signInAnonymously is not a function` 导致匿名登录失败的问题，增加方法存在性校验
  - 新增 `_authFailed` 标志位：匿名登录或云函数权限失败后自动标记云端不可用，后续所有调用直接回退 localStorage，避免反复报错
  - `API.call()` 增加 PERMISSION_DENIED 检测，收到权限错误时自动禁用云端模式
  - 版本号提升至 `?v=3.5`
- **V3.6（真正的任务分配系统）**：
  - 重构任务分配链路：导师分配的任务通过 `Storage.addTask()` 专用接口保存，云端模式使用 `addTask` 云函数（先查 `_id` 再 `doc().update()`），本地模式直接操作 localStorage
  - 修复 `Storage` 层所有云端方法：每个方法在云端调用失败（返回 null/error）时自动降级到本地模式，彻底杜绝"点了没反应"
  - 减少预生成模板任务：从生成到 currentWeek 改为只生成前 2 周基础任务，新增 `source: 'template'` 标记
  - 导师分配任务新增 `source: 'mentor'`、`assignedBy`、`assignedAt` 字段，支持可追溯
  - 导师分配界面增强：默认选中实习生当前周、增加自定义截止日期选择、显示已分配任务列表、支持删除任务
  - 实习生任务视图重构：从"只显示本周"改为"按周分组显示所有任务"，本周任务左侧蓝色高亮，任务卡片标记来源（基础任务/导师分配）
  - 新增 `deleteTask` 云函数和前端接口，导师可撤销已分配任务
  - 云函数 `updateIntern` 改为先 `get()` 查 `_id` 再 `doc(_id).update()`，解决 `where().update()` 对嵌套数组替换不稳定的问题
  - 版本号提升至 `?v=3.6`

**效果评估**：
- 实习生端：12周成长路线一目了然，任务完成率可视化，周记AI反馈解决"没人看"的痛点；导师评论直接显示在日志下方，形成成长对话链
- 导师端：带教计划模板一键应用，实习生状态卡片直观呈现，评价维度标准化+过程评语沉淀；逾期任务和未交日志自动提醒到通知中心
- HR端：全局数据大盘实时统计，多因子风险模型自动识别"评分低+日志拖欠+任务逾期+无评语"的复合风险人员；评分趋势折线图追踪单人能力成长曲线；数据可导出备份防止localStorage丢失

---

**Demo地址**：https://zuoye-d1gabtew72ef97c74-1436276735.tcloudbaseapp.com
